'use client';

export function LineChart() {
  return (
    <div style={{ height: 150, borderBottom: '2px solid var(--border-color)', borderLeft: '2px solid var(--border-color)', display: 'flex', alignItems: 'flex-end', padding: 10, gap: 10 }}>
      {/* Mock line chart structure */}
      <p style={{ color: 'var(--text-tertiary)', fontSize: '0.8rem' }}>Line Chart UI Placeholder</p>
    </div>
  );
}
