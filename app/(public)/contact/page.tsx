import ContactSection from '@/components/landing/ContactSection';

export default function ContactPage() {
  return (
    <div style={{ paddingTop: 80 }}>
      {/* Reusing the ContactSection we built earlier */}
      <ContactSection />
    </div>
  );
}
